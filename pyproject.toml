[project]
name = "mca21-risk-intelligence"
version = "1.0.0"
requires-python = ">=3.12"

[tool.vercel]
entrypoint = "backend.app.main:app"

[tool.vercel.scripts]
build = "cd frontend && npm run build"
